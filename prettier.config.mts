import { type Config } from 'prettier'

const config: Config = {
  arrowParens: 'always',
  endOfLine: 'lf',
  printWidth: 80,
  semi: false,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'es5',
}

export default config
